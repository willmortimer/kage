[package]
name = "kage-audit"
version = "3.0.0-alpha.1"
edition = "2021"

[dependencies]
kage-types = { path = "../kage-types" }
serde_json = "1.0"
dirs = "5.0"

[dev-dependencies]
tempfile = "3.23"
serde = { version = "1.0", features = ["derive"] }
